---
import type { TocItem } from "@/utils/toc";

interface Props {
  heading: TocItem;
}

const { heading } = Astro.props;
---

<li>
  <a href={"#" + heading.slug}>
    {heading.text}
  </a>
  {
    heading.subheadings.length > 0 && (
      <ul style={`margin-left: ${heading.depth * 10}px`}>
        {heading.subheadings.map((subheading) => (
          <Astro.self heading={subheading} />
        ))}
      </ul>
    )
  }
</li>
