---
interface Props {
  href?: string;
  class?: string;
}

const { href, class: className } = Astro.props;

const Element = href ? "a" : "div";
const extraProps = href ? { href } : {};
---

<Element
  class:list={["bg-muted opacity-75 hover:opacity-100", className]}
  {...extraProps}
>
  <slot />
</Element>
